// Placeholder for Mental Wellness feature

document.addEventListener('DOMContentLoaded', () => {
  const container = document.getElementById('mentalWellnessApp');
  if (!container) return;

  container.innerHTML = `
    <p class="text-gray-700">Mental Wellness features like mood tracking, guided meditation, breathing exercises, and CBT journaling will be implemented here.</p>
  `;
});
